{
  "name": "@open51/utils",
  "version": "1.0.0",
  "main": "index",
  "types": "index",
  "scripts": {
    "dev": "yarn rmrf && tsc -W -p tsconfig.json",
    "build": "yarn rmrf && tsc -p tsconfig.json",
    "rmrf": "rimraf dist",
    "pretty": "prettier --write src/**/*"
  },
  "license": "MIT"
}
