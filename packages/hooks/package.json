{
  "name": "@open51/hooks",
  "version": "1.0.1",
  "main": "index",
  "types": "index",
  "license": "MIT",
  "scripts": {
    "dev": "yarn rmrf && tsc -W -p tsconfig.json",
    "build": "yarn rmrf && tsc -p tsconfig.json",
    "rmrf": "rimraf dist",
    "pretty": "prettier --write src/**/*"
  },
  "dependencies": {
    "@open51/utils": "^1.0.1"
  },
  "peerDependencies": {
    "react": ">= 16.8.0"
  }
}
